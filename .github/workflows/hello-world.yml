name: hello-world
on: push 
jobs:
   hello-world:
   runs-on: ubuntu-latest
   steps:
    - name: check out repository code
      uses: actions/checkout@v3
    - run: "current_step=$(cat hello-world.txt)"
